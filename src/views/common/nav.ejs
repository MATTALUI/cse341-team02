<nav class="navbar navbar-dark bg-dark fixed-top navbar-expand-sm p-0">
  <div class="container">
    <a class="navbar-brand d-flex align-items-center" href="/">
      <!-- <img src="/icon.png" alt="" width="40rem" height="40rem"> -->
      <span class="fs-3 ms-1">Lizen</span>
    </a>
    <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
      <% if (currentUser) {%>
        <li class="nav-item">
          <a href="/cart-items" class="nav-link">
            <i class="fa fa-shopping-cart"></i>
            My Cart
            <span id="cart-count">
              <% try{%>
                <%= `(${cartItems.length})` %>
              <% }catch{%>
                <%= "(0)" %>
              <% }%>
            </span>
          </a>
        </li>
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            <%= currentUser.toString() %>
          </a>
          <ul class="dropdown-menu">
            <!-- <li>
              <a class="dropdown-item" href="/orders">My Orders</a>
            </li> -->
            <li>
              <hr class="dropdown-divider">
            </li>
            <li>
              <a href="/auth/logout" class="dropdown-item">Log Out</a>
            </li>
          </ul>
        </li>
      <% } else {%>
        <li class="nav-item">
          <a href="/auth/login" class="nav-link">Log In</a>
        </li>
      <% } %>
    </ul>
  </div>
</nav>
